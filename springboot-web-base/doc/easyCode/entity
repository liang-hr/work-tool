##导入宏定义
$!define

##保存文件（宏定义）
#save("/entity", ".java")

##包路径（宏定义）
#setPackageSuffix("entity")

##自动导入包（全局变量）
$!autoImport
import com.baomidou.mybatisplus.extension.activerecord.Model;
import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableField;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import java.io.Serializable;
import lombok.Data;
import lombok.ToString;
import lombok.NoArgsConstructor;

##表注释（宏定义）
#tableComment("实体类")
@NoArgsConstructor
@Data
@ToString(callSuper = true)
@TableName("$!{tableInfo.obj.name}")
@SuppressWarnings("serial")
public class $!{tableInfo.name} extends BaseModel<$!{tableInfo.name}> {

    #foreach($column in $tableInfo.fullColumn)
        #if(${column.name}== "id" || ${column.name}== "createTime"
        || ${column.name}== "updateTime" || ${column.name}== "creator" || ${column.name}== "updater")
        #else
            #if(${column.comment})
            /**
            * ${column.comment}
            */
            #end
            #if(${column.name} == ${tableInfo.pkColumn[0].name})
            @TableId(value = "${tableInfo.pkColumn[0].obj.name}",type = IdType.AUTO)
            private $!{tool.getClsNameByFullName($column.type)} $!{column.name};
            #else
            @TableField(value = "${column.obj.name}")
            private $!{tool.getClsNameByFullName($column.type)} $!{column.name};
            #end
        #end
    #end

}